plugins {
  id "com.moowork.node" version "1.2.0"
}

apply plugin: 'base'
version '0.0.1'

buildDir = 'dist'

node {
  version = '8.9.0'
  download = true
}

yarn_run_build.dependsOn(yarn_install)
assemble.dependsOn(yarn_run_build)
clean.dependsOn(yarn_run_clean)



